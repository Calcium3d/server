{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for the medical app",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js",
    "migrate": "npx prisma migrate dev",
    "studio": "npx prisma studio"
  },
  "author": "Calcium",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "dependencies": {
    "@apollo/server": "^4.5.0",
    "@prisma/client": "^4.12.0",
    "dotenv": "^16.0.3",
    "graphql": "^16.6.0",
    "postgres": "^3.3.4"
  },
  "devDependencies": {
    "prisma": "^4.12.0"
  }
}
